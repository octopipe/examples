name: example-push

trigger:
  on: 
    - push
  branches: ["feature-*"]

tasks:
  hello:
    steps:
      - image: alpine
        script: |
          #!/usr/bin/env sh

          echo "HELLO UNDERTABLE"
